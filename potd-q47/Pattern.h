#ifndef PATTERN_H
#define PATTERN_H

#include <string>
#include <iostream>
#include <utility>
#include <vector>
using std::pair;
using namespace std;

using namespace std;

bool wordPattern(string pattern, string str);
int find(const vector<pair<string, string>> &v, string str);
#endif
